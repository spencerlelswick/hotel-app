import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders Hotel App', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Hotel App/i);
//   expect(linkElement).toBeInTheDocument();
// });
